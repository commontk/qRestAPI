#
# This module is provided as qCDashAPI_USE_FILE by qCDashAPIConfig.cmake.  It can
# be INCLUDED in a project to load the needed compiler and linker
# settings to use qCDashAPI.
#

IF(NOT qCDashAPI_USE_FILE_INCLUDED)
  SET(qCDashAPI_USE_FILE_INCLUDED 1)
  
  # Add include directories needed to use qCDashAPI.
  INCLUDE_DIRECTORIES(${qCDashAPI_INCLUDE_DIRS})
  
ENDIF()
